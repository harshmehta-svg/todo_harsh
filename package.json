{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A simple Todo List application built with front-end technologies.",
  "main": "index.js", // Changed main field to a valid entry point (in this case, a JavaScript file)

  "scripts": {
    "start": "live-server",
    "build": "tsc --outDir build",
    "test": "jest",
    "clean": "rm -rf build && touch build",
    "remove-duplicates": "glob \"*.js\" && glob \"*.css\" && glob \"*.html\" && rm -rf styles.css && rm -rf index.html && rm -rf index.css && rm -rf app.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^16.11.3",
    "bcrypt": "^5.2.1",
    "typescript": "^4.7.4",
    "react": "^18.2.0", // React dependency
    "react-dom": "^18.2.0" // React DOM dependency
  },
  "devDependencies": {
    "@types/jest": "^29.4.0", // Update Jest types dependency
    "@types/jquery": "^3.6.2",
    "@types/live-server": "^1.2.7",
    "glob": "^7.2.7", // glob dependency to remove unnecessary files
    "jest": "^29.4.0", // Update Jest dependency
    "live-server": "^1.2.1",
    "@types/react": "^18.0.21", // Update React types dependency
    "@types/react-dom": "^18.0.12" // Update React DOM types dependency
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "bugs": {
    "url": ""
  }
}